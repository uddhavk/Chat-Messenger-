Chat Messenger with Log Facility is a Java-based peer-to-peer chat application built using Java Socket Programming (TCP/IP) and Swing GUI.

The application enables real-time text communication between a server and a client and automatically maintains a log file containing all conversation details with timestamps.

The project is platform-independent and runs on any system with Java Runtime Environment (JRE).
